# 默认本地虚构数据。命令行使用 uv run --env-file .env ... 才会加载此文件。
DATABASE_URL=sqlite:///./demo.db
TRACE_MODE=trace
SQL_ECHO=0
DEMO_DELAY_MS=0
LOG_FILE=server.log
# PostgreSQL：仅使用本机预先创建、名称以 m0_ 开头的隔离教学库。
# DATABASE_URL=postgresql+psycopg://<user>:<password>@localhost:5432/m0_tracer
