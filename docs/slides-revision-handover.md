# 课前两讲与第一课 slides 修订交接

## 1. 当前状态与下一步

**已完成三份 slides 与第一课入口的内容修改，最终源码的两章构建通过。尚未完成最后两处改动的浏览器复验，不能将本轮标成完整视觉验收通过。**

用户先要求依据修订后的教学底稿审查三份课件，随后明确批准：“我同意你的审查意见，请落地实施修改”。实施与首轮浏览器检查已经进行。最后一次浏览器复验被用户取消，接着用户要求关闭会话前编写本交接文档。

下次最小续接任务：
1. 复验讲一拆开的 HTML 文档页和表单页，确认首轮发现的裁切已经消除。
2. 复验第一课 U04 新增的“教师演示报文”标签。
3. 检查拆页后的相邻导航与讲二入口；若没有新问题，完成交付说明。

无需从头重做教学审查、重写底稿或修改 M0 代码；用户已批准本次修订范围。

## 2. 范围与有效教学约束

工作区：`/Users/wzl/PycharmProjects/web-development-slidev`。

本轮修改：
- [第一课](../pages/ch01/01.md)
- [课前讲一](../pages/ch00/00-lecture-one.md)
- [课前讲二](../pages/ch00/02-lecture-two.md)
- [第一课章节入口](../ch01.md)

已授权并持续有效的教学原则：
- 课前课负责“运行工具与代码阅读”；第一课负责“观察与取证”。
- 分层、依赖注入、框架设计留给后续课，不作为第一课先修考核。
- 课件可以详实，课堂不必逐页讲完；区分主讲、教师演示、思路导读、课后基础阅读和选做。
- A 档是基础必交，可课后完成；B 档是选做，不能互相替代。
- 第一课唯一学生现场独立任务是同一次执行的四处取证，保留 10 分钟。
- 不强迫 AI 出错、学生看不清日志或凑够错误数量。
- 第四课取消 20 分钟诊断小测；前四课均按 95 分钟教学＋5 分钟缓冲设计。
- 不回改历史反例来迁就后续契约，不把已有正确技术边界退回旧说法。

## 3. 本轮已落盘修改

### 第一课与章节入口

`pages/ch01/01.md`：
- 新增“课堂抓住一条主线，课后按卡片补齐”路线页，明确教师演示、学生任务、A/B 分工和时间边界。
- U02、U04、U05、U06、U07 标明教师演示；U08 标明唯一学生现场必做。
- 日志交织页不再写“制造痛感／学生现场必做”；学生能定位时说明依据，不要求必须失败。
- U09 从 6 分钟改为 16 分钟，11 个单元合计 95 分钟：`4+10+10+10+12+10+6+10+16+2+5`。
- “C 档速查”改为“课后结论卡”；工具卡、URL 和作业规格明确课后查阅。
- A 档明确包括链路图、四处记录、M0 验收；页面显示报障 id 保持 B 档。
- 新增“同一个案例，契约按阶段演进”交接卡，区分课堂 v1/v2、M0→第二课、第三课和第四课 R/E。
- SQL 预告改为第 3 次使用参数化模板、第 6 次讲原理、第 7 次 N+1、第 15 次攻防。
- “后续 101 次查询”改成依数据量和加载策略实测的 N+1 对照，不预设次数。
- `/boom` 预告细化为第四课补齐 finally 日志与外层 500 响应头，第五课深化时序；第一课仍保留故意缺陷。
- 备注明确只注册 handler 不保证旧中间件后半段执行。

`ch01.md`：
- 目录和结束页的下一课名称改成“HTTP 判据与请求—界面闭环”。
- 收尾说明第二课承接 GET `/questions` 与 `success/data`，补齐搜索页四种状态。

保留的正确内容：当前线程栈不等于完整请求链；SQL echo 默认无 rid；四处取证暂停并发；curl 重发是另一次执行；有无可见状态码均不能单独判定端点是否执行；M0 详情、探针、请求标识和验收范围不回退。

### 课前讲一

`pages/ch00/00-lecture-one.md`：
- 新增“讲一怎么学：先跑通工具，再按需查表”导读。
- 保留起停服务、curl／Network、工作目录、报错定位、断点与切帧的必做要求。
- HTML 标签、方法与状态全表标为认读参考，不要求逐项背诵。
- 请求头对比页不再承诺“第二次课整节课讲”，改为按搜索页需要运用报文判据。
- mini 页改为左右两栏：add/list 必做，完整 CRUD 流程参考；说明重复运行 id 会变化，不为固定输出清空数据。
- 读报错问题改为“先核对什么”，不借循环导入材料要求学生做架构重构。
- **首轮浏览器检查发现 HTML 页横向溢出与底卡裁切后，已再次修改：**
  - 拆为“0.2 HTML：浏览器眼里的文档”和“0.2 表单：一次提交发往哪里？”。
  - 表单代码换行，属性移到独立右栏，保留标签表和 input 类型参考。
  - 明确示例 `/posts` 只是阅读片段，不连接当前搜索服务，不要求实现端点。
  - 点击结论区分 POST 正文与 GET 查询串，说明以通过原生校验、可提交字段为前提。
  - 未靠缩小字号或隐藏溢出解决裁切。
- 最后这次 HTML 拆页已构建通过，**浏览器修复效果尚未复验**。

### 课前讲二

`pages/ch00/02-lecture-two.md`：
- 新增“讲二怎么学：会读，不要求从零写框架”导读。
- 包装器和 MiniApp 内部实现标为教师演示／课后回看。
- 原“暂停跟做 9”改为“阅读练习 9”，要求解释登记与调用时机，不要求独立实现框架；自行复演可选。
- 保留函数对象／调用的区别、带参数装饰器两步、注册／包装对照，以及 async/await 的适当阅读深度。
- 参数化预告同步为第三课用模板、第六课解释原理。
- 环境页明确只发一次请求，`/docs` 与 curl 二选一；课前不提前演示日志关联和四处取证。
- 保留 v1/v2 的旧 POST 路由，未全局替换成 GET。
- 配置页标为阅读参考；后续课程清单同步输入／输出契约、保契约重构与批准迁移。

## 4. 必须保持的跨课契约

| 阶段 | 核心约定 |
|---|---|
| 第一课课堂 v1/v2 | 旧 POST `/getQuestions`；`success/data`；用于历史反例与观察 |
| M0 → 第二课 | GET `/questions`，保留 `success/data`；第二课只修页面，不回退旧 POST |
| 第三课 | GET 路径不变；公开 `page_size`；列表改 `items/total/page`，含 author、不含 body；同步前端适配器与渲染 |
| 第四课 R | 纯重构，不改第三课方法、路径、参数、成功或错误输出 |
| 第四课 E | 批准普通 JSON 错误迁移为 `code/message/detail/request_id`，同步客户端与 OpenAPI |

M0 起持续保留 GET `/healthz` 的独立正文：
- 200：`{"status":"ok","db":"ok"}`。
- 503：`{"status":"degraded","db":"down"}`。
- 不套普通业务错误体。

本轮没有修改 `snippets/ch01/m0-tracer/` 的阶段源码、验证器、数据库或锁文件。

## 5. 已执行的检查及证据

### 构建与静态检查

最终 HTML 拆页与 U04 标签修改后，下列构建均成功退出：

```bash
pnpm exec slidev build ch00.md --out .build-check/slides-sync-ch00
pnpm exec slidev build ch01.md --out .build-check/slides-sync-ch01
```

实际环境显示 Slidev `52.19.1`、seriph、UnoCSS、Vite `8.2.2`。输出只有插件耗时提示，没有构建错误。

- `git diff --check` 在编写交接前再次通过。
- 核对第一课 11 个时间标签，合计 95 分钟。
- 已检索旧 C 档、固定 101 次查询、旧请求头预告等目标文案；本轮目标位置已修改。
- `GetProblems` 未能取得检查结果：语言服务未就绪。不能将其说成 lint 通过。
- 构建命令曾尝试 `--log error`，当前 build 子命令不支持；已去掉该参数重新成功构建。

### 首轮浏览器检查：完成

通过 Browser 子代理检查了两个真实章节入口中的指定修改页及关键动画。

- 实际视口 `936×598`、DPR 2，正文显示区域约 `936×527`；未能调整成计划的 1440×900。
- 字体加载后结合可见 DOM 与截图检查。
- 未发现资源加载失败或控制台错误。
- 第一课新增路线页、契约卡、日志提示、/boom、SQL 预告、U08、U09、U10、M0 规格与结尾正常。
- 课前两讲导读、请求头预告、mini 双栏、MiniApp 输出、参数化提示、环境与阅读边界正常。
- 抽查栈动画 0–5 点击、装饰器两步 0–2 点击、第一课搜索引用高亮，以及四处合流组件，结果正常。
- 唯一发现的是原 HTML 页：表单代码横向宽度 `508 > 422`，点击后底卡越界约 10px。已实施拆页修复，尚待复验。

### 截图位置与时效

截图由子代理保存在 `.build-check/`，该目录为本地验证产物，可能被搜索工具或 Git 忽略。不能据普通 Glob 无结果断言截图不存在；可按绝对路径读取。

以下文件已由主助手实际打开确认：
- [首轮 HTML 底卡裁切](../.build-check/browser-ch00-31-click1-bottom.png)：**修复前证据**，不能作为当前修复通过证据。
- [第一课版本交接卡](../.build-check/browser-ch01-45-contract.png)。

首轮子代理另外报告保存：
- `.build-check/browser-ch00-31-html-clipped.png`
- `.build-check/browser-ch00-42-mini-columns.png`
- `.build-check/browser-ch00-66-mini-output.png`
- `.build-check/browser-ch00-49-click4.png`
- `.build-check/browser-ch00-49-click5.png`
- `.build-check/browser-ch00-62-click2.png`
- `.build-check/browser-ch01-05-route.png`
- `.build-check/browser-ch01-26-click0.png`
- `.build-check/browser-ch01-26-click1.png`
- `.build-check/browser-ch01-32-sql.png`
- `.build-check/browser-ch01-35-evidence.png`
- `.build-check/browser-ch01-50-m0-check.png`
- `.build-check/browser-ch01-55-next.png`

**截图文件名中的页号是首轮检查时页号。HTML 拆页后，ch00 后续页号顺延，应按标题定位。**

### 最后一次复验：未完成

第二次 Browser 调用返回：`Agent execution was canceled by user`。没有修复后截图或检查结论，不应推断复验成功，也不用把该取消当成源码失败。

## 6. 下次具体续接清单

### 优先：收尾浏览器复验

1. 打开 ch00，按标题找到 HTML 文档页（预计第 31 页）及新增表单页（预计第 32 页）。
2. 检查文档骨架代码、标签表、说明均完整可见。
3. 检查表单代码最后字符、五个属性、input 类型参考，无横向裁切和底部溢出。
4. 表单页初始状态应隐藏结论，点击 1 后完整显示 POST／GET 说明。
5. 验证“HTML 文档 → 表单 → URL 解剖”前后翻页；目录仍能进入讲二，没有旧表单重复页。
6. 打开 ch01 的“先用 /docs 发一次真实请求”（预计第 15 页），确认 U04“教师演示报文”标签完整。
7. 保存修复后截图，明确记录实际视口；必要时再检查更大桌面视口。
8. 若为解决新问题继续编辑，重建对应章节并复验受影响页。
9. 执行 `git diff --check`，完成简短交付，说明未做 PDF／投影验收。

无需再次派 CodeReview 重复此前同一审查；当前缺口是浏览器渲染复验，不是重新设计课程。

### 当前本地预览服务

编写交接时用 `lsof` 确认以下进程仍监听：

| 入口 | URL | 当时 PID |
|---|---|---:|
| ch00 | http://localhost:3140/ | 84815 |
| ch01 | http://localhost:3141/ | 85108 |

两项均已提供 RunPreview。会话关闭后可能停止，PID 仅为当时记录，不能据此直接操作将来的进程。默认 3030／3031 当时已被占用，没有停止占用者。

需要时在项目根目录重新启动：

```bash
pnpm exec slidev ch00.md --port 3140 --bind 127.0.0.1
pnpm exec slidev ch01.md --port 3141 --bind 127.0.0.1
```

先确认端口与对应入口，不重复启动或结束其他服务。实际 `lsof` 显示监听 `::1`；优先使用上表已工作的 localhost 地址。

## 7. Git 状态与历史改动边界

编写交接前共有 12 个已跟踪文件未提交：
- 本轮 4 个：`ch01.md`、三份目标 slides。
- 之前任务留下 8 个：`docs/draft/lesson-01.md` 至 `lesson-07.md`，以及 `docs/syllabus.md`。

本轮没有再次修改上述八份底稿／大纲。其大量差异来自此前已完成的前四课整改与第 5–7 课优化，不能视为本轮意外覆盖，也不能为了整理 slides 而回滚。

本交接文档是额外新增文件。本轮未提交、未部署、未安装或升级依赖，未更改共享 CSS、Vue 组件或课堂互动系统。构建结果在 `.build-check/`，没有用 `pnpm build` 覆盖站点 `dist/`。

## 8. 更早验证与长期未验收项

此前底稿整改任务已完成：前四课正文、大纲和第五课交接同步；历史记录为 Python 95 项断言、Node 39 项断言、M0 `verify_m0.py --all` 三阶段通过。临时 Python／JS 验证脚本已经删除；不能将这些历史结果当作本轮重新运行，也不能直接执行已删除路径。

仍未完成、且不应混入本轮 slides 收尾承诺：
- 第 2–4 课完整独立教学工程与完整课堂回归包。
- PostgreSQL 创建／标签竞争／真实提交故障与数据升级集成。
- pydantic-settings、模板与表单的完整端到端集成。
- 搜索示例的浏览器 defer、键盘、Offline、安全渲染、缓存实验实测。
- 新一轮 M0 DevTools／IDE 界面取证、40 秒备用录像。
- 教室投影、PDF 导出及静态讲义完整性。

本轮浏览器检查针对 slides 的显示与点击，不是上述示例应用验收。课件附页中的“待补／未验证”说明仍然有效。
